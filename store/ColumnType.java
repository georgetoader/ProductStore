package store;

// Enum which contains the possible information a column may have
public enum ColumnType {
    UNIQUE_ID,
    PRODUCT_NAME,
    MANUFACTURER,
    PRICE,
    NUMBER_AVAILABLE_IN_STOCK
}